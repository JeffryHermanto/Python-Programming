# Virtual Environments in VSCode

# $ pipenv --venv
# Copy path and give it to the Code Runner extensions inside "code-runner.executerMap"

import requests

response = requests.get("http://google.com")
print(response)
